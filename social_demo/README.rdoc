== README


* Ruby version: 2.1.1

* Rails version: 4.1.5

The app database is sqlite3

It uses AngularJs for the javascript view engine

With ngRouse as the router

and Omniauth-facebook Gem in order to connect to facebook with koala gem for fetching the friends data

and angular_rails_csrf to avoid authentiaction problems with the rest post messages


